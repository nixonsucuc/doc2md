# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# macOS
.DS_Store

# Built apps and binaries — regenerate with the build.sh in each folder
*.app/
ocr/bin/

# Never commit credentials. The real key lives in ~/.config/doc2md/env, outside
# this tree; this line is belt-and-braces in case one is ever dropped in here.
env
.env
*.key
usage.json

# Claude Code per-machine local settings
.claude/settings.local.json

# Local test corpus. Useful scans to develop the layout and OCR work against,
# but they are pages of published books — this repository is public, so they
# stay on the machine that produced them. Keep your own here; nothing in the
# tests depends on it, and the fixtures they do use are generated in-process.
Sample Docs/
